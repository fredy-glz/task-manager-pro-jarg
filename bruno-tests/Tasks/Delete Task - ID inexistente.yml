info:
  name: Delete Task - ID inexistente
  type: http
  seq: 16

http:
  method: DELETE
  url: "{{baseUrl}}/api/tasks/9999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
