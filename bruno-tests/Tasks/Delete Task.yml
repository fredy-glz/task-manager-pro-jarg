info:
  name: Delete Task
  type: http
  seq: 15

http:
  method: DELETE
  url: "{{baseUrl}}/api/tasks/11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
