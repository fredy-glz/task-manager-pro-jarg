info:
  name: Get Task - ID inexistente
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/api/tasks/9999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
