info:
  name: Get Tasks - Sin token
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/tasks"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
