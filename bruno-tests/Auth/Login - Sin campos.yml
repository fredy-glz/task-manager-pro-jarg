info:
  name: Login - Sin campos
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:3000/api/auth/login
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
