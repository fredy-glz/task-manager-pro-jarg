info:
  name: Update Task - ID inexistente
  type: http
  seq: 13

http:
  method: PUT
  url: "{{baseUrl}}/api/tasks/9999"
  body:
    type: json
    data: |-
      {
        "status": "in_progress",
        "priority": "critical"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
