info:
  name: Create Task - Status inválido
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/tasks"
  body:
    type: json
    data: |-
      {
        "title": "Tarea válida",
        "status": "INVALIDO"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
